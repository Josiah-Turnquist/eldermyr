// scripts/split-monolith.mjs — RETIRED (P1 wrap).
//
// This script's job — slicing the frozen monolith (eldermyr-rpg.html) into the positional
// part files under src/game/parts/ at parser-verified statement boundaries — ran once and
// ended when the monolith was deleted. The parts + src/game/manifest.json ARE the source
// now; `npm run build` assembles dist/eldermyr.html from them (plus the build-generated
// Eldermyr namespace epilogue). The monolith, and the version of this script that could
// split it, live on in the `v2-final` tag.
throw new Error('split-monolith: the monolith era is over — src/game/parts/ is the source (see v2-final tag for the original).');
